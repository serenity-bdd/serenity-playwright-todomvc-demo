# Serenity BDD Configuration for Playwright Tests

serenity {
    # Project name displayed in reports
    project.name = "TodoMVC Playwright Demo"

    # Take screenshots only on failures
    take.screenshots = FOR_FAILURES

    # Report output directory
    outputDirectory = target/site/serenity
}